# python bytecode from talk/effort_router_demo.py
__pycache__/
*.pyc

# scratch
.DS_Store
*.log

# playwright MCP drops page/console dumps here when driving the browser
.playwright-mcp/

# holds literal r2mcp credentials (DB URL, Supabase token, OpenRouter key) — never commit
.mcp.json
